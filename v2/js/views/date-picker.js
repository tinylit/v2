require(['components/v2.date-picker'], function (datePicker) {
    datePicker({
        min: new Date(),
        //max: new Date()
        //format:"yyyy-MM-dd"
    });
    datePicker({
        //min: new Date(),
        max: new Date()
        //format:"yyyy-MM-dd"
    });
    datePicker({
        min: new Date(),
        max: new Date()
        //format:"yyyy-MM-dd"
    });
});
